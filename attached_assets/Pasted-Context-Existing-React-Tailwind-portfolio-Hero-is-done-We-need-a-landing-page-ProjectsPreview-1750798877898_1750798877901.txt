Context – Existing React + Tailwind portfolio; Hero is done. We need a landing-page “ProjectsPreview” section with two work examples that reveal a sliding overlay on hover.

Files to add / modify

/src/data/projectsPreview.json

json
Copy
Edit
[
  {
    "id": "skingenius",
    "title": "Skingenius – AI Skincare Recommender",
    "role": "Product Manager • 2024",
    "thumb": "/thumbnails/skingenius.jpg",
    "caption": "Boosted engagement 30% by redesigning onboarding & quiz flow.",
    "link": "/projects/skingenius"
  },
  {
    "id": "nomad",
    "title": "Nomad AI – SMB Valuation SaaS",
    "role": "PM-Consultant • 2024",
    "thumb": "/thumbnails/nomad.jpg",
    "caption": "Projected 42% lift in conversions with new dynamic pricing model.",
    "link": "/projects/nomad"
  }
]
(Place two jpg placeholders in /public/thumbnails/ for now.)

/src/components/ProjectsPreview.jsx

jsx
Copy
Edit
import projects from '../data/projectsPreview.json';
import { motion } from 'framer-motion';

const Card = ({ p }) => (
  <a href={p.link} className="group relative block w-full h-80 overflow-hidden rounded-2xl shadow-lg">
    {/* background image */}
    <img src={p.thumb} alt={p.title} className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110" />

    {/* sliding overlay */}
    <motion.div
      initial={{ y: '100%' }}
      whileHover={{ y: 0 }}
      transition={{ type: 'spring', stiffness: 120, damping: 14 }}
      className="absolute inset-0 bg-ink/90 dark:bg-base/90 text-base text-base-100 p-6 flex flex-col justify-end"
    >
      <h3 className="text-xl font-semibold mb-2">{p.title}</h3>
      <p className="text-sm opacity-80 mb-3">{p.role}</p>
      <p className="text-sm leading-snug">{p.caption}</p>
    </motion.div>
  </a>
);

const ProjectsPreview = () => (
  <section id="work" className="py-28">
    <div className="container mx-auto px-6">
      <h2 className="text-4xl lg:text-5xl font-semibold mb-12">
        A tiny&nbsp;fraction&nbsp;of&nbsp;my&nbsp;work
      </h2>
      <div className="grid gap-10 md:grid-cols-2">
        {projects.map(p => <Card key={p.id} p={p} />)}
      </div>
    </div>
  </section>
);

export default ProjectsPreview;
Import the section in App.jsx (or Landing page component) below the Hero:

jsx
Copy
Edit
import ProjectsPreview from './components/ProjectsPreview';
…
<Hero />
<ProjectsPreview />
…
Styling notes
• Uses Tailwind colours ink / base already in your config.
• group-hover:scale-110 adds the zoom-in effect on the background.
• Overlay slides up via Framer Motion (y from 100 % → 0).
• Entire card is a link to full case-study page or modal.

Accessibility – alt on images, focus ring OK via Tailwind defaults.

Commit message suggestion
"Add hover-reveal ProjectsPreview section (tiny fraction of my work)"

